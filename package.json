{
  "name": "ralph-cube-navigation",
  "version": "1.0.0",
  "description": "Interactive cube navigation with parallax backgrounds",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint .",
    "test": "echo \"No tests specified\" && exit 0",
    "postinstall": "npm run build",
    "build": "echo \"No build step required\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "compression": "^1.7.4",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "eslint": "^8.56.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
} 